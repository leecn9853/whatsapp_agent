# content_agent（实验目录）

> **当前阶段可忽略**：本目录为独立实验/原型，**不参与**主链路（`whatsapp_simulator` → `excel_agent` → `third_app`）。  
> 本地跑 Demo 请从仓库根目录 [README.md](../README.md) 开始。

若需单独探索，可参考 `content_agent/.env.example` 与 `Makefile`。
